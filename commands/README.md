# Plugin-Compatible Commands

This directory mirrors `.claude/commands/` so other tools that prefer a top-level `commands/`
layout can discover the same `/modu` workflows.

If your tool supports `.claude/commands/`, prefer that.
